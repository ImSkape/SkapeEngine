name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  # -- WINDOWS ------------------------------------
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Configure CMake - Game
        run: cmake -B build-game -G "Visual Studio 17 2022" -A x64 -DBUILD_EDITOR=OFF

      - name: Build Game Debug
        run: cmake --build build-game --config Debug -- /m

      - name: Run Game Debug headless
        shell: cmd
        run: build-game\game\Debug\Game.exe --headless

      - name: Build Game Release
        run: cmake --build build-game --config Release -- /m

      - name: Run Game Release headless
        shell: cmd
        run: build-game\game\Release\Game.exe --headless

      - name: Configure CMake - Editor
        run: cmake -B build-editor -G "Visual Studio 17 2022" -A x64 -DBUILD_EDITOR=ON

      - name: Build Editor Debug
        run: cmake --build build-editor --config Debug -- /m

      - name: Run Editor Debug headless
        shell: cmd
        run: build-editor\editor\Debug\Editor.exe --headless

      - name: Build Editor Release
        run: cmake --build build-editor --config Release -- /m

      - name: Run Editor Release headless
        shell: cmd
        run: build-editor\editor\Release\Editor.exe --headless

  # -- MAC ----------------------------------------
  build-mac:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Configure CMake - Game
        run: |
          cmake -B build-game \
            -G "Xcode" \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=10.14 \
            -DBUILD_EDITOR=OFF

      - name: Build Game Debug
        run: cmake --build build-game --config Debug

      - name: Find Game executables
        run: find build-game -name "Game" -o -name "Game.exe" 2>/dev/null

      - name: Run Game Debug headless
        run: ./build-game/game/Debug/Game --headless

      - name: Build Game Release
        run: cmake --build build-game --config Release

      - name: Run Game Release headless
        run: ./build-game/game/Release/Game --headless

      - name: Configure CMake - Editor
        run: |
          cmake -B build-editor \
            -G "Xcode" \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=10.14 \
            -DBUILD_EDITOR=ON

      - name: Build Editor Debug
        run: cmake --build build-editor --config Debug

      - name: Find Editor executables
        run: find build-editor -name "Editor" -o -name "Editor.exe" 2>/dev/null

      - name: Run Editor Debug headless
        run: ./build-editor/editor/Debug/Editor --headless

      - name: Build Editor Release
        run: cmake --build build-editor --config Release

      - name: Run Editor Release headless
        run: ./build-editor/editor/Release/Editor --headless

  # -- LINUX --------------------------------------
  build-linux:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake \
            ninja-build \
            libgl1-mesa-dev \
            libglu1-mesa-dev \
            libx11-dev \
            libxrandr-dev \
            libxinerama-dev \
            libxcursor-dev \
            libxi-dev \
            libwayland-dev \
            wayland-protocols \
            libxkbcommon-dev \
            pkg-config \
            xvfb

      - name: Build Game Debug
        run: |
          cmake -B build-game -G "Ninja" \
            -DCMAKE_BUILD_TYPE=Debug \
            -DBUILD_EDITOR=OFF
          cmake --build build-game

      - name: Find Game executables
        run: find build-game -type f -executable 2>/dev/null

      - name: Run Game Debug headless
        run: xvfb-run ./build-game/game/Game --headless

      - name: Build Game Release
        run: |
          cmake -B build-game-release -G "Ninja" \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_EDITOR=OFF
          cmake --build build-game-release

      - name: Run Game Release headless
        run: xvfb-run ./build-game-release/game/Game --headless

      - name: Build Editor Debug
        run: |
          cmake -B build-editor -G "Ninja" \
            -DCMAKE_BUILD_TYPE=Debug \
            -DBUILD_EDITOR=ON
          cmake --build build-editor

      - name: Run Editor Debug headless
        run: xvfb-run ./build-editor/editor/Editor --headless

      - name: Build Editor Release
        run: |
          cmake -B build-editor-release -G "Ninja" \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_EDITOR=ON
          cmake --build build-editor-release

      - name: Run Editor Release headless
        run: xvfb-run ./build-editor-release/editor/Editor --headless